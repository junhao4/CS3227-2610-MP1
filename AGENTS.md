# Instructions

- Read `specs/MP1-requirements.md`.
- Follow the relevant repository skill in `.agents/skills/` and use `logging` if possible for all AI use.
- Ask before adding dependencies, publishing issues, committing, or pushing.
