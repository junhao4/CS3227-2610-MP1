package cs3227.moneymap.domain;

/** Identifies whether money enters or leaves the user's records. */
public enum TransactionType {
    /** Money entering the user's records. */
    INCOME,
    /** Money leaving the user's records. */
    EXPENSE
}
